import {FC} from 'react';

import NotFoundPageContainer from './NotFoundPage.styled';

const NotFoundPage: FC = () => {
  return <NotFoundPageContainer>Page not found</NotFoundPageContainer>;
};

export default NotFoundPage;
